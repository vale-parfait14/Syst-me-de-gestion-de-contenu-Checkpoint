const Media = require('../models/Media');

// Fonction pour uploader un média
const uploadMedia = async (req, res) => {
  // ... implémentez votre logique pour uploader un média ici
};

// Fonction pour obtenir tous les médias
const getMedia = async (req, res) => {
  // ... implémentez votre logique pour obtenir les médias ici
};

module.exports = { uploadMedia, getMedia };
